import { GoogleGenerativeAI } from '@google/generative-ai';\n\n// Access your API key as an environment variable (preferable in production)\nconst apiKey = process.env.GEMINI_API_KEY;\n\nif (!apiKey) {\n  // This check should ideally be done at app startup or build time in a more robust app\n  // For this MVP, we'll let the API route handle the error gracefully if it's missing at runtime.\n  console.warn(\"GEMINI_API_KEY is not set. AI functionalities will be disabled.\");\n}\n\n// Initialize the Google Generative AI\nconst genAI = new GoogleGenerativeAI(apiKey || 'dummy-key-if-missing'); // Pass a dummy key if missing to avoid immediate crash, error handled in route\n\n// For text-only input, use the gemini-pro model\nconst model = genAI.getGenerativeModel({ model: 'gemini-pro' });\n\nexport { model };